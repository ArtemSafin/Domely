apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: task-service
  namespace: homie
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: task-service
                port:
                  number: 8080
